# REGISTER

The project register is the work_items table in Supabase (project jfaaqzrezcrkkidlsbwj), ~400 rows, IDs SS-xxx, permanent, never deleted — superseded only. Writers: Claude (chat) writes rows after Racquel's explicit approval (R23). Code never writes work_items directly (R24); Code outputs REGISTER PROPOSAL blocks that Claude verifies and lands. Constraints resolved_needs_evidence and ui_claims_need_screenshot enforce evidence rules in Postgres. Rows from either Claude session are expected and are not an intrusion. Racquel owns all closures.
